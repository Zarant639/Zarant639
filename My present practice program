#include <stdio.h>
#include <stdlib.h>

char z;
int main(void) {
    printf("Enter the number: ");
    scanf("%d", z);
    
    //file check
    FILE *file = fopen("number.txt", "a");
    if (file == NULL) {
        printf("ERROR");
        return 1;
    }
    fprintf(file, "%d");
    fclose(file);
    return 0;
}
